# Logo assets (lpweb)

Canonical lockup for LAUNCHPAD Admissions Intelligence. Prefer the PNG raster as the web source of truth.

| File | Description |
|------|-------------|
| `logo-horizontal-color.png` | Approved raster lockup (transparent; brand purple ~`#75458E`) |
| `logo-horizontal-color.svg` | Vector lockup (kept for export; site headers use PNG) |
| `logo-horizontal-white.svg` | Full lockup, white (dark backgrounds) |
| `logo-mark-rocket.svg` | Star + rocket mark only |

Web paths: `images/logo-launchpad.png` (primary), `images/logo-launchpad.svg` (optional vector copy), root `favicon.svg` (rocket mark).

Header HTML uses the PNG lockup sitewide (see `scripts/cutover_www_and_logo.py`).
