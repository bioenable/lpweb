# Logo assets (lpweb)

Canonical lockup for LAUNCHPAD Admissions Intelligence.

| File | Description |
|------|-------------|
| `launchpad-new-logo-transparent.png` | Source of truth — approved transparent raster lockup |

**Served site path (canonical):** `images/logo-launchpad.png?v=3`  
(identical bytes to `launchpad-new-logo-transparent.png`)

Favicons at repo root (`favicon.svg`, `.ico`, PNGs, `apple-touch-icon.png`) are the rocket mark only — not the full lockup.

Header HTML uses the PNG lockup sitewide (see `scripts/cutover_www_and_logo.py`).
